import { FC } from 'react'
import { Recover_form } from './Recover_form'

export const Recover_page: FC = () => {
  return (
    <div className='flex justify-center h-[100vh]'>
        <Recover_form/>
    </div>
  )
}